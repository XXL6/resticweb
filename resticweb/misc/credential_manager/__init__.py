from resticweb.tools.credential_manager import CredentialManager

# this file's just used to initialize the credential manager
# as initializing it in other places might cause import errors

credential_manager = CredentialManager()